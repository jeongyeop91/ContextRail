# Agent guide

1. Read `docs/README.md`, then only documents relevant to the task.
2. Read `state/CURRENT.md` and its item in `state/BACKLOG.json`.
3. Work through `search -> locate -> bounded read -> modify -> targeted validation`.
4. Keep active authority below 500 lines and the router below 50 lines.
5. Preserve user changes and never commit secrets, transcripts, or host-specific paths.
